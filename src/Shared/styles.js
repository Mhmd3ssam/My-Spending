import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  inputContainer: {
    marginBottom: 16,
  },
  label: {
    marginBottom: 4,
    fontWeight: 'bold',
  },

  centerLabelWrapper: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  centerLabelValue: {
    fontSize: 22,
    color: '#232B5D',
    fontWeight: 'bold',
  },
});
